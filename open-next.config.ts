// open-next.config.ts
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Minimal configuration for Cloudflare
export default defineCloudflareConfig({
  // No additional configuration needed for basic setup
});
